const a = true
const b = true

describe('Test', () => {
  test('Test a and b', () => {
    expect(a).toEqual(b);
  });

  test('Test a and b', () => {
    expect(b).toEqual(a);
  });

  test('Test a and b', () => {
    expect(b).toEqual(a);
  });
  
  test('Test a and b', () => {
    expect(b).toEqual(a);
  });
});
